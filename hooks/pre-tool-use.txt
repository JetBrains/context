Tip: if the relevant file is still unknown, use EmbArk for one semantic bootstrap search, then open and inspect at least one returned file before any second search. Only search again if that local inspection failed; on CLI use `embark search -p <path> ...`, and on MCP set `pathFilter`.
