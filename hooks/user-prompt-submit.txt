Tip: when the relevant file is unknown, try one EmbArk semantic search first, then open and inspect at least one returned file before any second search. Only search again if that local inspection failed; on CLI use `embark search -p <path> ...`, and on MCP set `pathFilter`.
