The path filter to restrict search to specific directories or a file. Use it after the first useful hit to narrow repeated searches to the relevant area. Must be a path relative to the project root. If not provided, the whole project is searched. Examples: "path/to/module", "path/to/module/submodule", "path/to/file/example.kt".
